<Window x:Class="Enigma.Frontend.Charts.ChartPositionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Enigma.Frontend.Charts"
        mc:Ignorable="d"
        Title="ChartPositions" Height="500" Width="900">
    <Grid Margin="6">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30*"/>
            <ColumnDefinition Width="70*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="35*"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>
        <Menu Grid.Row="0" Name="ChartMenu" Height="22" Grid.ColumnSpan="2" HorizontalAlignment="Left" VerticalAlignment="Top">
            <MenuItem Header="MiGeneral" Name="MiGeneral">
                <MenuItem Header="MiClose" Name="MiClose"/>
            </MenuItem>
            <MenuItem Header="MiGraphics" Name="MiGraphics">
                <MenuItem Header="MiDefaultWheel" Name="MiDefaultWheel"/>
            </MenuItem>
        </Menu>
        <TextBlock Grid.Row="1" Grid.ColumnSpan="2" Margin="6" Name="FormTitle" Text="FormTitle" Style="{StaticResource titleStyle}" />
        <TextBlock Grid.Row="2" Margin="6" Name="ChartName" Text="ChartName"/>
        <TextBlock Grid.Row="3" Margin="6" Name="Description" Text="Description"/>
        <TextBlock Grid.Row="4" Margin="6" Name="Location" Text="Location"/>
        <TextBlock Grid.Row="5" Margin="6" Name="DateTime" Text="DateTime"/>
        <TextBlock Grid.Row="6" Margin="6" Name="ChartCartegory" Text="ChartCategory"/>
        <TextBlock Grid.Row="7" Margin="6" Name="Rating" Text="Rating"/>
        <TextBlock Grid.Row="8" Margin="6" Name="Source" Text="Source"/>

        <DataGrid Grid.Row="2" Margin="6" Grid.Column="1" Grid.RowSpan="7" Name="DGHouses" AlternatingRowBackground="AliceBlue" AlternationCount="2" AutoGenerateColumns="True" 
                  ScrollViewer.CanContentScroll="True" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Auto">
        </DataGrid>

        <DataGrid Grid.Row="9" Margin="6" Grid.ColumnSpan="2" Name="DGCelPoints" AlternatingRowBackground="AliceBlue" AlternationCount="2" AutoGenerateColumns="True" 
                  ScrollViewer.CanContentScroll="True" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Auto">

        </DataGrid>
        <StackPanel Grid.Row="10" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="BtnHelp" Content="BtnHelp" Margin="6"/>
            <Button Name="BtnClose" Content="BtnClose" Margin="6"/>
        </StackPanel>
        
    </Grid>

    
    
</Window>
